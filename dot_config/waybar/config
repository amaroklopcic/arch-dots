{
    "signals": {
        "custom/darkmodetoggle": 10
    },

    "layer": "top",

    // TODO: add disk, network usage, etc.
	"modules-left": [
        "custom/launcher",
        "cpu",
        "memory",
        "network",
        "disk"
    ],
	"modules-center": ["sway/workspaces"],
	"modules-right": [
        "custom/updates",
        "custom/darkmodeswitch",
        "pulseaudio",
        "clock",
        "tray"
    ],

    "custom/darkmodeswitch": {
        "format": "<span font='12px'>\udb81\udd0e</span>   {}",
        "exec": "$HOME/.local/bin/theme-icon.sh",
        "interval": 9999,
        "tooltip": "Toggle Dark/Light Theme",
        "on-click": "$HOME/.local/bin/toggle-theme.sh",
    },

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": false
    },

	"pulseaudio": {
		"tooltip": false,
		"scroll-step": 5,
		"format": "<span font='12px'>{icon}</span>   {volume}%",
		"format-muted": "<span font='12px'>\ueee8</span>   {volume}%",
		"on-click":"pactl set-sink-mute @DEFAULT_SINK@ toggle",
		"format-icons": {
			"default": ["\uf026", "\uf027", "\uf028"]
		}
	},

	"network": {
		"tooltip": false,
		"format-wifi": "<span font='12px'>\uf1eb</span> {essid}",
		"format-ethernet": "<span font='12px'>\udb80\udf17</span>   {ipaddr}/{cidr}",
        "format-disconnected": "<span font='12px'>\udb80\udf19</span>"
	},

	"tray":{
		"icon-size":16,
		"spacing": 14
	},

    "clock": {
        "format": "{:%H:%M %d/%m/%Y}"
    },

    "cpu": {
        "interval": 1,
        "format": "<span font='12px'>\uf4bc</span>   {load:0.1f}%",
    },

    "memory": {
        "interval": 30,
        "format": "<span font='12px'>\uefc5</span>   {}%",
    },

    "disk": {
        "path": "/",
        "interval": 30,
        // "format": "<span font='12px'>\udb80\udeca</span>   {percentage_used}%",
        "format": "<span font='12px'>\udb80\udeca</span>   {specific_used:0.1f}<span font='11px'>GiB</span> / {specific_total:0.1f}<span font='11px'>GiB</span> ({percentage_used}% used)",
        // "format": "<span font='12px'>\uf0a0</span>   {used} ({percentage_used}%) / {total}",
        "tooltip": true,
        "tooltip-format": "{used} out of {total} used ({percentage_used}%)",
        "unit": "GB"
    },

    // TODO: clicking this multiple times opens multiple launchers
    "custom/launcher":{
        "format": "<span font='16px'>\udb82\udcc7</span>  ",
        "on-click": "tofi-drun"
    },

    "custom/updates":{
        "format": "{} update(s)",
        "exec": "paru -Qu | wc -l",
        "exec-if": "[[ $(paru -Qu | wc -l) != 0 ]]",
        "interval": 60,
        "on-click": ""
    }
}
